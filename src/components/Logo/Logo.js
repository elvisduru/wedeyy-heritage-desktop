import React from "react";
import logo from "../../images/Wedeyy Connect.png";

const Logo = props => (
  <img
    src={logo}
    width={props.width}
    height={props.height}
    alt="Wedeyy Connect"
  />
);

export default Logo;
